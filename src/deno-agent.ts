/**
 * Deno-compatible agent bridge
 * Re-exports the same runSimpleAgent function to ensure full parity
 */

// Re-export the same runSimpleAgent function to ensure full parity
export { runSimpleAgent } from './simple-agent.js';
